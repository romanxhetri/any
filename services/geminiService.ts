
import { Movie } from "../types";

// This file formerly contained AI logic. 
// It is kept as a placeholder to prevent import errors if referenced, 
// but the actual logic is removed to rely on local data.

export const searchMoviesWithGemini = async (query: string): Promise<Movie[]> => {
    return [];
};
